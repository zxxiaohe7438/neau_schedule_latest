import type { ImportResult } from '../domain/ImportResult';

/**
 * Import course data from clipboard text or HTML.
 * Placeholder implementation.
 */
export function importClipboard(_text: string): ImportResult {
  // TODO: implement clipboard import
  // Should detect if input is HTML or plain text and delegate accordingly
  throw new Error('Clipboard importer not yet implemented — will be completed in Day 4');
}
