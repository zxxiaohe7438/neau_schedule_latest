# 测试计划

## 单元测试

### 现有测试

- 剪贴板导入器（6 个）
- HTML 导入器（5 个）
- JSON 规范化（normalizer，7 个）
- CSV/Excel 导入器（5 个）
- 导出器（tools/neau-timetable-exporter，16 个）
- 学校课表解析器（schoolIndexImporter，14 个）— 2026-08-03 恢复并校准
- 学校会话文件（schoolSessionFile，8 个）— 2026-08-03 新增
- 学校抓取服务（schoolFetchService，10 个，注入 mock fetch）— 2026-08-03 新增
- 合计 71 个用例

> 说明：2026-08-01 重构（移除联网功能）删除了 mock callback/auth/school index 相关测试；
> 2026-08-03 新增官网抓取功能时从 git 历史还原了 schoolIndexImporter 与其 fixture（mock-callback-response.json），
> 并将测试改写为始终运行的 fixture 断言。

## 集成测试

- IPC 通信
- 导入 → 预览 → 确认流程
- 备份 → 恢复流程
- 官网抓取：登录窗口 → 会话接管 → 抓取 → 解析 → 预览（**开学后实机联调**）

## 手动验收

### 基础功能

- [x] npm install 成功
- [x] npm run dev 能启动
- [x] 能看到桌面窗口
- [x] 能创建学期
- [x] 重启后学期仍存在
- [x] 能导入 JSON
- [x] 能在课表中显示课程
- [x] 能编辑课程
- [x] 断网后仍能使用

### 数据导入

- [ ] 导入 JSON 文件 → ImportPreview 显示课程 → 确认后进入课表
- [ ] 导入 HTML 文件（课表页面另存为）
- [ ] 剪贴板粘贴课程文本（格式：课程名 教师 地点 星期 节次 周次）
- [ ] 智能粘贴：粘贴考试时间文本（如 "6月15日(周一) 14:00-16:00 数据库原理"）
- [ ] 重复导入同一文件 → 显示去重跳过
- [ ] 手动修改过的事件重新导入 → 显示冲突，可选择保留/覆盖/跳过
- [ ] 冲突选择"保留本地"→ 不产生重复事件（2026-08-03 修复）
- [ ] 调课/换地点后重新导入 → 同格重叠提示冲突（2026-08-03 新增检测）

### 官网获取（开学后实机联调）

- [ ] 点击"官网获取"→ 登录窗口弹出（学校统一身份认证页）
- [ ] 扫码登录 / 账号密码登录 → 登录成功后窗口自动检测并关闭
- [ ] 点击"获取课表"→ 抓取成功 → 学期信息确认（开学日期/周数可改）→ 预览
- [ ] 重复抓取 → 去重/冲突处理正常（含调课后同格冲突裁决）
- [ ] 重启应用后会话保持（记住登录）→ 直接抓取
- [ ] 清除登录 → 状态复位
- [ ] 校外 WebVPN 环境：先登录 WebVPN → 登录窗口 → 抓取（候选域自适应）
- [ ] 开发模式"模拟数据"按钮：离线完整流程演示

### 备份与恢复

- [ ] 学期卡片点击"备份" → 选择位置导出 JSON
- [ ] 首页点击"恢复备份" → 选择 JSON 文件 → 恢复成功提示
- [ ] 恢复前自动生成备份文件

### 提醒系统

- [ ] 打开设置（⚙️）→ 调整提前分钟数/通知方式 → 保存
- [ ] 课程事件设置自定义提醒 → 到点触发 Toast
- [ ] 桌面系统通知在 Windows 正常弹出
- [ ] 测试 Toast 按钮正常工作

### 数据保护

- [ ] 手动修改标记 updated_manually
- [ ] 重新导入不静默覆盖手动修改
- [ ] 官网抓取：密码不经过应用代码（登录在独立窗口完成）、会话文件为加密内容、关闭"记住登录"即清除
