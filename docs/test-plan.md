# 测试计划

## 单元测试

### 现有测试

- 剪贴板导入器（6 个）
- HTML 导入器（5 个）
- JSON 规范化（normalizer，6 个）
- CSV/Excel 导入器（5 个）
- 导出器（tools/neau-timetable-exporter，16 个）

> 说明：Mock callback 导入器、Auth service mock、学校 index 导入器、学校 callback 导入器相关测试
> 已随 2026-08-01 重构（移除联网功能）一并删除。

## 集成测试

- IPC 通信
- 导入 → 预览 → 确认流程
- 备份 → 恢复流程

## 手动验收

### 基础功能

- [x] npm install 成功
- [x] npm run dev 能启动
- [x] 能看到桌面窗口
- [x] 能创建学期
- [x] 重启后学期仍存在
- [x] 能导入 JSON
- [x] 能在课表中显示课程
- [x] 能编辑课程
- [x] 断网后仍能使用

### 数据导入

- [ ] 导入 JSON 文件 → ImportPreview 显示课程 → 确认后进入课表
- [ ] 导入 HTML 文件（课表页面另存为）
- [ ] 剪贴板粘贴课程文本（格式：课程名 教师 地点 星期 节次 周次）
- [ ] 智能粘贴：粘贴考试时间文本（如 "6月15日(周一) 14:00-16:00 数据库原理"）
- [ ] 重复导入同一文件 → 显示去重跳过
- [ ] 手动修改过的事件重新导入 → 显示冲突，可选择保留/覆盖/跳过

### 备份与恢复

- [ ] 学期卡片点击"备份" → 选择位置导出 JSON
- [ ] 首页点击"恢复备份" → 选择 JSON 文件 → 恢复成功提示
- [ ] 恢复前自动生成备份文件

### 提醒系统

- [ ] 打开设置（⚙️）→ 调整提前分钟数/通知方式 → 保存
- [ ] 课程事件设置自定义提醒 → 到点触发 Toast
- [ ] 桌面系统通知在 Windows 正常弹出
- [ ] 测试 Toast 按钮正常工作

### 数据保护

- [ ] 手动修改标记 updated_manually
- [ ] 重新导入不静默覆盖手动修改
- [ ] 密码/凭据相关内容已全部移除（无登录功能）
