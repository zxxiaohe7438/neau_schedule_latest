# 2026-08-04 开发日志

## 完成内容

按 `F:\skills\` 中的「精简 Skill 分类提示词」（skill-selection.json + 给ZCode_精简安装提示词.md）执行 ZCode Skill 精简安装：

1. **核验 Skill 真实名称**（GitHub API，4 个仓库）：
   - addyosmani/agent-skills：7 个目标 Skill 名称全部精确匹配（`skills/<name>/SKILL.md`，仅含 SKILL.md）
   - gamedev-skills/awesome-gamedev-agent-skills：gamedev-router 实际目录为 `router`；引擎为子 Skill 组（godot 15 个 / unity 8 个 / unreal 6 个 / web-engines 6 个）；pygame 为 `pygame-core`、Bevy 为 `bevy-ecs`
   - huggingface/skills：3 个名称精确匹配（huggingface-datasets / huggingface-community-evals / huggingface-llm-trainer）
   - Orchestra-Research/AI-research-SKILLs：全部小写（litgpt/nanogpt/mamba/torchtitan/megatron-core/deepspeed）；FSDP 实际为 `pytorch-fsdp2`

2. **安装全局 4 个** → `C:\Users\LEGION\.zcode\skills\`（用户级，所有项目生效）：
   planning-and-task-breakdown、debugging-and-error-recovery、code-review-and-quality、source-driven-development

3. **安装项目级 3 个** → `D:\coding_relative\neau-local-schedule\.zcode\skills\`（本项目为软件项目，用户确认立即安装）：
   spec-driven-development、frontend-ui-engineering、api-and-interface-design

4. **生成脚本包** `D:\coding_relative\neau-local-schedule\zcode-skill-profiles\`：
   global-core.ps1、software-project.ps1、game-project.ps1、model-training-project.ps1、model-building-project.ps1、README.md
   - 脚本使用 `git clone --sparse --filter=blob:none` 稀疏检出完整 Skill（含 references/）
   - 安装前检查存在性（存在即跳过）；失败即停止并输出原因；UTF-8 with BOM 防 PowerShell 5.1 中文乱码

## 改动文件

- 新增：`C:\Users\LEGION\.zcode\skills\{4 个全局 Skill}\SKILL.md`
- 新增：`D:\coding_relative\neau-local-schedule\.zcode\skills\{3 个项目级 Skill}\SKILL.md`
- 新增：`zcode-skill-profiles\{5 个 ps1 + README.md}`

## 测试结果

| 测试项 | 结果 |
|---|---|
| 7 个 SKILL.md frontmatter `name` 字段 | ✅ 全部匹配 |
| 5 个 ps1 PowerShell 解析检查 | ✅ 全部 PARSE OK |
| global-core.ps1 实跑（已存在跳过路径） | ✅ 4 个 SKIP，正常完成 |
| software-project.ps1 实跑（已存在跳过路径） | ✅ 3 个 SKIP，正常完成 |
| game-project.ps1 -Engine pygame 实跑（真实下载，临时目录） | ✅ router/game-ui-ux/pygame-core 安装完整（含 references/） |
| model-training-project.ps1 -Type vision 实跑 | ✅ datasets + community-evals 安装完整 |
| model-building-project.ps1 默认实跑 | ✅ litgpt + nanogpt 安装完整（含 references/），frontmatter name=nanogpt |
| 错误路径：distributed 缺 -Distributed | ✅ 抛出明确错误 |
| 错误路径：vision 带 -Distributed | ✅ 抛出明确错误 |
| 错误路径：game 缺 -Engine | ✅ MissingMandatoryParameter |
| 临时目录清理 | ✅ 冒烟测试目录已删除 |

## 风险与说明

- **设计类 7 个 Skill（banner-design/brand/design/design-system/slides/ui-styling/ui-ux-pro-max）仍留在全局目录**：提示词要求全局只放 4 个，但禁止事项不允许删除已有 Skill，故保留并仅报告。
- `code-review-and-quality` 原本已存在于 `C:\Users\LEGION\.agents\skills\`（2026-06-02 安装，同源同版本），本次在 `.zcode\skills` 重复安装一份（`.zcode` 优先级更高，内容一致，无冲突）。
- 名称映射（router/gamedev-router、pytorch-fsdp2/FSDP、全小写命名）已写入 README 和脚本注释，脚本动态按组拉取引擎子 Skill，不硬编码未确认目录。
- 全局 Skill 的物理位置在 C 盘用户目录（加载规则固定扫描位置）；ZCode 数据目录 `dataBaseDir` 在 D 盘（D:\zcode_data），项目级 Skill 因项目在 D 盘而位于 D 盘。

## 下一步建议

- 若需为游戏/模型训练/模型构建项目装 Skill，进入对应项目后运行 `zcode-skill-profiles\` 下对应脚本即可（按需安装，不自动创建 Profile）。
- 可考虑将 zcode-skill-profiles/ 随仓库提交，方便团队复用。

## 追加：打包当前版本

- 执行 `npm run build`（tsc 类型检查 + vite 构建 + electron-builder NSIS 打包），exit code 0
- 产物：`release/NEAU Local Schedule Setup 0.1.0.exe`（84 MB，NSIS 安装包）、`release/win-unpacked/NEAU Local Schedule.exe`（免安装版）、blockmap 与 builder-debug.yml
- 测试：vitest 8 个文件 71 个用例全部通过
- 说明：未配置应用图标（使用默认 Electron 图标）、未签名；package.json 缺少 author 字段（electron-builder 仅提示不影响打包）
