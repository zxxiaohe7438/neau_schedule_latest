# 2026-07-04 开发日志

## 完成内容

1. 梳理了项目当前结构和主流程，确认了 Electron 主进程、preload、React 渲染层、sql.js 数据库和 browser-act 在线抓课表链路的分工。
2. 定位并修复了“不能直连学校官网自动导入课表”的入口问题：
   - 原来的登录入口走的是 mock 校验流程
   - 现在自动登录会直接调用学校官网抓课接口，并在成功后进入导入预览
   - browser-act 不可用时保留本地降级流程
3. 进一步把半自动流程补成“先打开学校官网、用户手动登录并跳转、再确认后开始爬取”的模式。
4. 统一修正了启动页和顶部栏的按钮文案，避免用户看到乱码式按钮文本。
5. 补齐了顶部栏残留的“导入 JSON”乱码按钮。
6. 清理了 `browserActSchoolService.ts` 里几个导致 `typecheck` 失败的未使用变量。

## 改动文件

- `src/components/LoginPanel.tsx`
- `src/app/App.tsx`
- `electron/services/browserActSchoolService.ts`
- `docs/dev-log/2026-07-04.md`

## 测试结果

- `npm.cmd run typecheck`
- 结果：通过
- `npm.cmd run test`
- 结果：9 个测试文件通过，86 个测试通过，8 个测试跳过

## 风险

- 这次修复把登录主入口切到了“直连官网并自动导入”，但真实学校网站在验证码、页面结构或网络状态变化时仍可能需要半自动兜底。
- `docs/CURRENT_STATUS.md` 可能还存在和代码实现不同步的描述，后续最好再对齐一次。

## 下一步建议

1. 如果要继续增强体验，可以给自动导入流程补一层更明确的失败提示和重试入口。
2. 如果要继续做功能开发，建议先同步更新 `docs/CURRENT_STATUS.md`，把已完成状态和实际代码对齐。
